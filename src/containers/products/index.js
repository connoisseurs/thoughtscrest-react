import React from 'react'
import { Button } from 'react-bootstrap'

const Products = props => (
    <div>
        <h1>Products</h1>
        <p>Page to display the list of products.</p>
        <Button bsStyle="danger">Danger</Button>
    </div>
)

export default Products;
